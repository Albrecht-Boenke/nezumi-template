# ToggleGroup Molecule — Token Audit

Tokens actually consumed by the component implementation.

| Token | Tailwind Utility Used | In `ui/styles/components/toggle-group.css` |
|-------|----------------------|--------------------------------------------|
| `--spacing-toggle-group-gap` | `gap-toggle-group-gap` | Yes |

> Note: ToggleGroup reuses `toggleVariants` from the Toggle atom. All Toggle tokens are listed in `atoms/Toggle/TOKENS.md`.
